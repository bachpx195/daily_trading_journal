<%= form_with(model: symbolfx, local: true, html: {class: "form-horizontal"}) do |form| %>
  <% if symbolfx.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(symbolfx.errors.count, "error") %> prohibited this symbolfx from being saved:</h2>
      <ul>
        <% symbolfx.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <span class="col-sm-1 btn btn-primary nohover" id="name">Tên</span>
    <div class="col-sm-11">
      <%= form.text_field :name, autocomplete: "off", id: "name", class: "form-control" %>
    </div>
  </div>

  <div class="form-group">
    <span class="col-sm-1 btn btn-primary nohover" id="slug">Viết tắt</span>
    <div class="col-sm-11">
      <%= form.text_field :slug, autocomplete: "off", id: "slug", class: "form-control" %>
    </div>
  </div>

  <div class="form-group">
    <span class="col-sm-1 btn btn-primary nohover" id="base">Base currency</span>
    <div class="col-sm-11">
      <%= form.text_field :base, autocomplete: "off", id: "base", class: "form-control" %>
    </div>
  </div>

  <div class="form-group">
    <span class="col-sm-1 btn btn-primary nohover" id="quote">Quote currency</span>
    <div class="col-sm-11">
      <%= form.text_field :quote, autocomplete: "off", id: "quote", class: "form-control" %>
    </div>
  </div>

  <div class="form-group">
    <span class="col-sm-1 btn btn-primary nohover" id="quote">Tỉ lệ thắng %</span>
    <div class="col-sm-11">
      <%= form.number_field :winrate, autocomplete: "off", id: "winrate", class: "form-control" %>
    </div>
  </div>

  <div class="form-group">
    <span for="title" class="col-sm-1 btn btn-primary nohover">
      Tag
    </span>
    <div class="col-sm-4">
      <%= form.select :tag_id, options_for_select(nested_set_options(Tag) {|i| "#{'__'* (i.level.to_i).abs} #{i.slug.present? ? i.slug : 'root'}" }, symbolfx.id.present? ? symbolfx.tag.id : params[:parent]) ,
                      {}, { :class => 'form-control select2_category' } %>
    </div>
  </div>

  <div class="form-group">
    <span class="col-sm-1 btn btn-primary nohover" id=content">Nội dung</span>
    <div class="col-sm-11">
      <%= form.cktext_area :desciption, rows: 10, id: "editor1" %>
    </div>
  </div>

  <div class="form-actions">
    <div class="row">
      <div class="col-sm-2">
        <button type="submit" class="btn btn-info">Lưu</button>
      </div>
    </div>
  </div>
<% end %>